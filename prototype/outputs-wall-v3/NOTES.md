# Open-Lens 成品墙 v3 — 说明

入口 `index.html`(双击或 `python3 -m http.server` 均可,相对路径取图)。
**真实数据**:218 张课堂投影照片(`data.js` + `assets/`,由 `gen-assets.py` 从 `spike/photos-batch` 生成;assets 已 gitignore,勿提交)。

## 与 v2 的关系

保留 v2「审计台」A 变体的全部交互契约,视觉与实现重做;B/C 变体废弃(v2 目录留存档)。

v3.1 调整(用户反馈后):

- 布局:左右两栏各自顶天 —— 过滤条件并入左栏头部,右栏自上而下为 头(文件名/状态/视图切换/重标/点选)→ 数据条 → opBar → 舞台图片
- 角点把手:v2 式绿点(`#30d158` + 白边),final 线宽 4 / proposal 虚线 4
- **点选改点**(默认开):点击画面任意处,离点击位置最近的角点直接移动过去,不用按住拖
- **重标 4 角**:点「重标 4 角」按钮,或**按住 ⌘/Ctrl 直接点击**开始;依次点 4 角成框,过程中带序号圆点 + 虚线轨迹,未满 4 点保存禁用;拖动把手随时可用
- 键盘:`↑↓` 切图(跨页)、`v` / `←` / `→` / `⌥↑↓` 原图↔成品(←→ 不翻页,防误触)、`⌘S/⌃S` 保存、`⌘Z/⌃Z` 撤销到打开时快照;翻页只用分页条(◀▶/跳页)

契约清单(改动需同步 #5 issue):

- 左 46% 分页列表:页大小按视口自适应(行高 44px),◀▶ + 跳页 + ←/→ 翻页,↑/↓ 跨页导航,列头排序,状态下拉 + 仅看 edited + 仅看偏差>2px
- 右舞台一屏一图零滚动:原图·角点 ↔ 成品 二态切换(`v` 或 ⌥↑/↓),proposal 蓝色虚线 / final 绿色实线 + 可拖把手
- opBar:自动保存开关(默认开,切图/翻页前自动提交 dirty)+ 撤销到打开时快照 + 保存(dirty 时变琥珀色)
- 标题「OpenLens 批量处理工作台」

v3 修复的 v2 硬伤:舞台高度塌陷图片不渲染(验收脚本只查 DOM 未查出)、抽屉未开时右侧空屏、随机艳色假缩略图。

## 数据派生

- `status`:ar 可疑带 = 中位数 1.9585 ±0.04 → [1.918, 1.998](6 张 suspect / 41 edited / 171 ok)
- `devPx` = final 与 proposal 四角点最大位移(label 1000×750 坐标系)
- 保存后重算 ar/status(出带 → suspect,入带 → edited)
- batch-meta 里 `IMG_4096.PNG`/`IMG_4097.PNG` 是大写扩展名,gen-assets.py 已兼容

## 验收

`verify.mjs(随分支存档)` 12 项断言(点选默认开/←→ 切视图不翻页/点选移点/⌘Z 撤销/⌘点击重标成框/⌘S 保存/分页按钮)全过。
